export type PaginationProps = {
    totalPages: number;
    currentPage: number;
    itemsPerPage: number;
};
export default function Pagination({ currentPage, totalPages, }: PaginationProps): import("react/jsx-runtime").JSX.Element;
